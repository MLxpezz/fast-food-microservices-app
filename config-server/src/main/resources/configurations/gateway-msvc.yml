server:
  port: 8080

spring:
  application:
    name: gateway-msvc
  cloud:
    gateway:
      discovery:
        locator:
          enabled: true
      routes:
        - id: users-msvc
          predicates:
            - Path=/api/v1/**
          uri: http://localhost:9080

